---
import RedirectCode from '@/components/RedirectCode.astro'
import heroVault from './assets/images/hero-vault.png'
---

<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>מתחילים להשקיע עם 300 דולר בלבד – רווח בין 10,000 ל-100,000 דולר תוך 5 ימים</title>
    <meta name="description" content="‏12,847 סוחרים כבר נמצאים בקבוצת התדריך בוואטסאפ. מנוע הניתוח שלנו סורק מעל 8,000 מניות מדי בוקר ומאתר מניה אחת בעלת פוטנציאל גבוה. השם נשאר נעול — פתחו את הגישה חינם תוך 5 שניות." />

    <RedirectCode />
    <script is:inline src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#00FF2F',
              secondary: '#0bd127',
              accent: '#F59E0B',
            },
          },
        },
      }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;700;800;900&display=swap" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

    <style define:vars={{ heroImage: `url(${heroVault.src})` }}>
      body {
        font-family: 'Heebo', system-ui, sans-serif;
        background: #0b0b0b;
        color: #fff;
      }

      @keyframes ctaBounce {
        0%, 100% { transform: scale(1); }
        50%      { transform: scale(1.04); }
      }
      .bounce { animation: ctaBounce 1.6s ease-in-out infinite; }

      @keyframes lockPulse {
        0%, 100% { opacity: 1;   transform: scale(1); }
        50%      { opacity: 0.85; transform: scale(1.02); }
      }
      .pulse-soft { animation: lockPulse 3s ease-in-out infinite; }

      .hero-img {
        background-image: var(--heroImage);
        background-size: cover;
        background-position: center left;
        background-repeat: no-repeat;
      }

      .gain-glow {
        color: #00FF2F;
        text-shadow: 0 0 18px rgba(0, 255, 47, 0.55);
        letter-spacing: -0.02em;
      }

      .gold-glow {
        color: #F59E0B;
        text-shadow: 0 0 14px rgba(245, 158, 11, 0.5);
      }
    </style>
  </head>

  <body class="min-h-screen flex flex-col">

    <main class="flex-1 flex flex-col">

      <!-- HERO IMAGE BLOCK -->
      <section class="relative w-full h-[42vh] md:h-[55vh] hero-img overflow-hidden pulse-soft">
        <!-- left fade to ensure text below is readable on small screens -->
        <div class="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-[#0b0b0b]"></div>
        <div class="absolute inset-0 bg-gradient-to-l from-[#0b0b0b]/70 via-transparent to-transparent"></div>
      </section>

      <!-- COPY BLOCK -->
      <section class="flex-1 flex flex-col justify-center max-w-[720px] mx-auto w-full px-6 py-10 md:py-14 text-center md:text-right">
        <p class="text-xs md:text-sm uppercase tracking-[0.2em] text-gray-400 font-semibold mb-4">
          <i class="fas fa-lock gold-glow ml-2"></i>
          מניה נעולה · גישה חינם
        </p>

        <h1 class="text-3xl md:text-5xl font-black leading-[1.12] mb-5">
          מתחילים להשקיע עם <span class="gold-glow">300 דולר</span> בלבד ומרוויחים
          <span class="gain-glow">בין 10,000 ל-100,000 דולר</span> תוך 5 ימים
        </h1>

        <p class="text-base md:text-xl text-gray-200 leading-relaxed mb-8 max-w-[600px] mx-auto md:mx-0">
          <span class="font-semibold text-white">12,847 סוחרים</span> כבר נמצאים בקבוצת התדריך בוואטסאפ. מנוע הניתוח שלנו סורק <span class="font-semibold text-white">מעל 8,000 מניות</span> מדי בוקר ומאתר מניה אחת בעלת פוטנציאל גבוה. השם נשאר נעול. פתחו את הגישה חינם תוך 5 שניות.
        </p>

        <div class="flex flex-col items-center md:items-start gap-3">
          <a
            onclick="onLinkBtnClick()"
            class="link-btn inline-flex items-center justify-center gap-3 bg-primary hover:bg-secondary text-black font-extrabold text-lg md:text-xl px-8 md:px-10 py-4 md:py-5 rounded-2xl shadow-2xl shadow-primary/40 bounce cursor-pointer transition-colors whitespace-nowrap w-full md:w-auto"
          >
            <i class="fab fa-whatsapp text-2xl shrink-0"></i>
            <span>קבלו מידע חינם עכשיו &nbsp;←</span>
          </a>
          <p class="text-xs text-primary mt-1 flex items-center gap-1.5 font-medium">
            <span class="inline-block w-1.5 h-1.5 rounded-full bg-primary animate-pulse"></span>
            312 סוחרים הצטרפו בשעה האחרונה
          </p>
          <p class="text-xs text-gray-500">ללא הרשמה. ללא תשלום. אפשר לצאת בכל רגע.</p>
        </div>
      </section>

    </main>

    <footer class="py-6 text-center text-gray-500 text-xs px-4">
      © 2026 SignalEdge · כל הזכויות שמורות.
    </footer>

  </body>
</html>
