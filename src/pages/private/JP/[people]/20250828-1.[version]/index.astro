---
import type { GetStaticPaths } from 'astro'
import BaseLayout from '../../../../../layouts/BaseLayout.astro'

export const getStaticPaths = (() => {
  return [
    { params: { people: '村上世彰', version: '0' }, props: { name: '村上世彰', img_prefix: 'cssz_' } },
    { params: { people: '広瀬隆雄', version: '0' }, props: { name: '広瀬隆雄', img_prefix: 'llx_' } },
    { params: { people: '测试器', version: '0' }, props: { name: 'テスタ', img_prefix: 'csq_' } },
    { params: { people: '大前研一', version: '0' }, props: { name: '大前研一', img_prefix: 'dqyy_' } },
    { params: { people: '永濱利廣', version: '0' }, props: { name: '永濱利廣', img_prefix: 'yb_' } },
    { params: { people: 'kenmoさん', version: '0' }, props: { name: 'kenmoさん', img_prefix: 'ken_' } },
    { params: { people: 'ひろゆき', version: '0' }, props: { name: 'ひろゆき', img_prefix: 'aa_' } },
    { params: { people: '三橋貴明', version: '0' }, props: { name: '三橋貴明', img_prefix: 'sq2_' } },
  ]
}) satisfies GetStaticPaths

const { name: people } = Astro.props
---

<BaseLayout title={people} description={people} track={true}>
  <!-- Hero Section -->
  <div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-green-50 px-4 py-8">
    <div class="max-w-md mx-auto text-center">
      <!-- 主标题 -->
      <h1 class="text-3xl font-bold text-gray-900 mb-4 leading-tight">
        市場は<span class="text-red-600">待ってくれません</span>
      </h1>

      <!-- 副标题 -->
      <p class="text-lg text-gray-700 mb-6 leading-relaxed">
        小さなサインを見逃すと、<br >
        次の急伸は誰かの手に渡ってしまうかもしれません
      </p>

      <!-- 价值主张 -->
      <div class="bg-white rounded-2xl p-6 mb-8 shadow-lg">
        <div class="flex items-center justify-center mb-4">
          <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
            <span class="text-2xl">📈</span>
          </div>
        </div>
        <h2 class="text-xl font-semibold text-gray-900 mb-3">
          成長が期待される注目株
        </h2>
        <p class="text-gray-600 text-sm leading-relaxed">
          専門家が分析した見逃せないサインを<br >
          <span class="font-semibold text-green-600">LINEで限定配信中</span>
        </p>
      </div>

      <!-- 社会证明 -->
      <div class="bg-gradient-to-r from-green-50 to-blue-50 rounded-2xl p-6 mb-8 border border-green-200">
        <div class="flex items-center justify-center mb-3">
          <span class="text-2xl">🎯</span>
        </div>
        <p class="text-gray-700 text-sm">
          登録すれば、動き始める前に<br >
          <span class="font-bold text-green-700">チャンスを先取り</span>できます
        </p>
      </div>

      <!-- 行动按钮 -->
      <div class="mb-8">
        <button
          class="link-btn inline-block w-full bg-green-500 hover:bg-green-600 text-white font-bold text-xl py-4 px-8 rounded-full shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 breathing"
        >
          📱 今すぐLINEで友達追加
        </button>
        <p class="text-xs text-gray-500 mt-3">
          ※完全無料・登録不要・いつでも解除可能
        </p>
      </div>

      <!-- 紧迫感区域 -->
      <div class="bg-red-50 border border-red-200 rounded-2xl p-6 mb-8">
        <div class="flex items-center justify-center mb-3">
          <span class="text-2xl">⏰</span>
        </div>
        <h3 class="text-lg font-bold text-red-700 mb-2">
          迷わず今すぐ！
        </h3>
        <p class="text-red-600 text-sm">
          情報を受け取って、<br >
          次のチャンスを逃さないでください
        </p>
      </div>

      <!-- 页脚 -->
      <div class="text-center">
        <p class="text-xs text-gray-400">
          © 2024 投資情報配信サービス
        </p>
      </div>
    </div>
  </div>
</BaseLayout>
