---
import BaseHead from '@/components/BaseHead.astro'
import RedirectCode from '@/components/KRRedirectCode.astro'
import { Filters, generateKrConfig } from '@/utils/kr_config'
import Body from './body.jsx'

export function getStaticPaths() {
 return generateKrConfig(Filters.byPerson('金永益'))
}

---
<html lang="ko">
	<BaseHead />
	<RedirectCode />
	<script>
		function jump() {
		  if (typeof gtag_report_conversion === 'function') { gtag_report_conversion(link) }
		  else { window.location.href = link }
		}
	</script>
<body>
<Body client:only="react" />


</body>

</html>
